
export class CategoryValidators {
    // static addCategories() {
    //     return [
    //     body("categoryImages", "Category image is required")
    //         .custom((category, { req }) => {
    //         if (!req.file) throw new Error("File not uploaded");
    //         else return true;
    //         }),
    //     ];
    // }
}
